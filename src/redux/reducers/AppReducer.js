const initialState = {
    loading: true
};

function appReducer(state = initialState, action) {
    // switch (action) {
    //     default:
    //         return state
    // }

    return state;
}

export default appReducer;